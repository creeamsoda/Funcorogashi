# フンコロバトル 実装計画（MVP）

> 本ドキュメントは実装の進行台帳。**トークン切れ時は Codex がこれを読んで続行する**前提で、環境・契約・進捗を常に最新化する。
> 仕様の一次ソース: [フンコロバトル_企画書.md](フンコロバトル_企画書.md) / [フンコロバトル_MVP構成.md](フンコロバトル_MVP構成.md)

最終更新: 2026-09-19 / 現在フェーズ: **I3〜I6完了 / 残り F(仮SE) → 4人通しプレイテストでMVP完成**

---

## 1. 概要

- 見下ろし3Dの4人ローカル対戦パーティーゲーム。フンを転がして育て、突進で奪い合う。1プレイ3分。
- **MVPはコアループの面白さ検証が目的**。見た目より「バランス調整可能な状態でループが回ること」を優先。
- **3Dモデルは本実装しない**：フン＝sphere、その他オブジェクト＝cube（BoxMesh）の変形で表現。

---

## 2. 開発環境（重要・Codex必読）

| 項目 | 値 |
|---|---|
| エンジン | Godot **4.7.2 stable**（標準版 / GDScript。mono版は使わない） |
| Godot実行ファイル | `C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64.exe` |
| コンソール版(stdout取得用) | `C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64_console.exe` |
| プロジェクト | `C:\Users\sotak\Develop\funcorogashi`（OneDrive外＝同期干渉なし） |
| GPU/描画 | Vulkan Forward+（Intel Arc で動作確認済） |

**PATH注意**: PATHに追加されていても実行ファイル名が `godot` でないため `godot` コマンドでは呼べない。上記フルパスを使う。

### 起動コマンド（PowerShell）
```powershell
$godot = "C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64_console.exe"
$proj  = "C:\Users\sotak\Develop\funcorogashi"

# class_name を追加/変更したら必ず import パスを先に走らせる（グローバルクラス登録）
& $godot --headless --path $proj --import

# headless で数フレーム実行して boot 検証
& $godot --headless --path $proj --quit-after 5
```

**落とし穴**: `class_name` 付きスクリプトを新規追加した直後は、先に `--import` を走らせないと
「Could not find type ... in the current scope」パースエラーになる。追加後は必ず import → 実行の順。

### 見た目テスト用ハーネス（操作不要のビジュアル確認をClaudeが自動化）
`tools/ScreenshotRunner.tscn` が対象シーンを描画→PNG保存→自動終了する。
```powershell
& $godot --path $proj "res://tools/ScreenshotRunner.tscn" -- `
  "--scene=res://scenes/Stage.tscn" `
  "--out=C:/temp/shot.png" "--frames=10"
# --scene 省略時は内蔵デモ(床cube+フンsphere)を描画。--out は絶対パス推奨。
```
※実行時に一瞬ウィンドウが開く（headlessだと描画されないため通常起動を使う）。

---

## 3. ビジュアル方針（モデルなし・プリミティブ変形）

| 対象 | 表現 |
|---|---|
| フン（育成対象・地面ドロップ） | `SphereMesh`。半径 = size × `size_to_scale` |
| プレイヤー本体（フンコロガシ） | 平たい `BoxMesh`。プレイヤー4色で識別 |
| 鳥 | 横長 box（ゆっくり直線横断） |
| 恐竜ラプトル | 細め box（素早く走り回る） |
| オランウータン | 大きめ box（ステージ外配置、フン球を投擲） |
| ステージ床・壁 | box（`StaticBody3D` + `CollisionShape3D`） |
| 突進当たり判定 | 不可視 `Area3D`（box） |

§5のフンらしさUI（飛沫・湯気等）は方針保持のみで**MVP実装なし**。
複雑なシーンはできるだけ `.tscn` 手書きを避け、**GDScriptでノード生成**（transform計算ミスやuid衝突を回避。primitivesのみなので問題なし）。

---

## 4. 突進仕様（確定・改訂版）

突進距離＝**無限**。壁かプレイヤーのどちらかに必ず当たるまで正面へ直進。最初に当たった対象で分岐：

| 当たった対象 | 結果 |
|---|---|
| 壁 | **自滅**（自分が保有フンの `drop_ratio`(既定50%) をドロップ ＋ スタン） |
| **突進中**のプレイヤー | **相打ち**（両者ノックバック ＋ 両者ドロップ） |
| それ以外（非突進中）のプレイヤー | **命中**（相手がドロップ ＋ スタン、自分は無傷） |

「狙った相手が避ける → そのまま壁まで直進して自滅」で回避の駆け引きが成立。
実装は `move_and_collide` を `dash_speed` でループし最初の接触対象で判定（`dash_max_time` を安全弁に）。

---

## 5. プロジェクト構成

```
funcorogashi/
├─ project.godot                     # [autoload] Config を登録済
├─ .gitignore
├─ resources/
│  └─ balance_default.tres           # BalanceConfig の既定インスタンス（調整はここ）
├─ scripts/
│  ├─ balance_config.gd  ✅          # class_name BalanceConfig（全パラメータ）
│  ├─ config.gd          ✅          # autoload。Config.balance で参照
│  ├─ main.gd            ✅(暫定)    # エントリポイント（I1でコアループ実装）
│  ├─ game_manager.gd    ⬜          # タイマー/勝敗/表彰集計/spawn_pickup
│  ├─ player.gd          ⬜          # 移動・状態機械・突進
│  ├─ dung_pickup.gd     ⬜          # 地面のドロップフン
│  ├─ animal_spawner.gd  ⬜
│  └─ animals/ bird.gd raptor.gd orangutan.gd thrown_dung.gd  ⬜
├─ scenes/
│  ├─ Main.tscn          ✅(暫定)
│  ├─ Stage.tscn         ⬜  Player.tscn ⬜  DungPickup.tscn ⬜
│  ├─ animals/*.tscn     ⬜
│  └─ ui/ HUD.tscn ⬜  Results.tscn ⬜
├─ tools/
│  ├─ screenshot_runner.gd ✅  ScreenshotRunner.tscn ✅
└─ audio/                            # 仮SE（F/後半）
```

---

## 6. F3 契約（並列作業の衝突を防ぐインターフェース）

### 6-1. パラメータ（実装済: `scripts/balance_config.gd`）
`Config.balance.<name>` で全域参照。主なもの: `match_duration, move_speed_base, size_gain_per_pickup,
size_growth_mode, size_max, size_to_scale, dash_cooldown, dash_windup, dash_speed, stun_duration,
dash_knockback_speed, drop_ratio, drop_scatter_count, pickup_despawn_enabled, animal_spawn_interval_min/max,
bird_*, raptor_*, orangutan_*`。詳細は同ファイル参照。

### 6-2. シグナル/API 規約（各トラックはこれに従って実装）
- **DungPickup**（`Area3D`）
  - プロパティ: `size: float`, `owner_id: int = -1`, `collectible_at: float`（この時刻以降、owner本人も回収可）
  - `signal collected(size: float)` を出して `queue_free()`
  - 生成: `GameManager.spawn_pickup(pos: Vector3, size: float, owner_id := -1)`
- **Player**（`CharacterBody3D`）
  - `player_id: int`, `size: float`, `facing: Vector3`
  - `enum State { ROLLING, DASH_WINDUP, DASHING, STUNNED }`, `state: State`
  - `signal size_changed(player_id, size)`, `signal dashed(player_id)`, `signal dash_hit(attacker_id, victim_id)`
  - `func add_size(amount: float)` / `func drop_and_stun(is_self_crash: bool)`
- **GameManager**（Main直下）
  - `players: Array[Player]`, `time_left: float`, `stats[player_id] = {dash_hits:int, pickups:int}`
  - `signal time_changed(time_left)`, `signal match_ended(results: Array)`
  - `results` 要素: `{player_id, size, dash_hits}`（降順ランキング＋サブ表彰算出用）
  - `func spawn_pickup(pos, size, owner_id := -1)`（"pickups"コンテナへ生成）
- **Animals**: ドロップは `GameManager.spawn_pickup(...)` を呼ぶだけ（プレイヤー/突進に非依存）
- **HUD**: GameManager の `time_changed` と各Playerの `size_changed` を購読
- **Results**: `match_ended(results)` を受けて順位＋サブ表彰1つ（突進命中数1位）を表示

### 6-3. 入力規約（I2）
- プレイヤー`player_id`→Joy-Conデバイスidを対応付け、`Input.get_joy_axis(device, JOY_AXIS_LEFT_X/Y)`と
  `Input.is_joy_button_pressed(device, JOY_BUTTON_A)`（突進）をポーリング。
- 開発中の単体テスト用に player0 のキーボード代替（WASD＋Space）を用意。
- **実機Joy-Conのデバイス割当確認はユーザーがエディタ＋実機で行う**（要操作テスト）。

---

## 7. 依存関係と並列トラック

```
F1 Godot導入(ユーザー)┐
F2 足場(Claude) ──────┼─→ F3 契約定義(Claude) ＝ 並列の起点
                      ┘        │
   ┌──────────┬──────────┬─────┼──────────┬──────────┐
   ▼          ▼          ▼     ▼          ▼
 【A】       【B】       【C】  【D】       【E】
 ステージ    プレイヤー   ドロップ 動物3種    UI(モック)
 /カメラ     移動+成長    回収    +スポナー   HUD/Results
 🖼️Claude   🎮ユーザー   🖼️/🎮  🖼️Claude   🖼️Claude
   │          │          │      │(要C)      │
   └────┬─────┴────┬─────┘      │           │
        ▼          ▼            │           │
     I1 コア統合🎮 ◄────────────┘(動物投入)  │
        ▼                                   │
     I2 4人入力+カメラ🎮（要InputMap）        │
        ▼                                   │
     I3 突進システム🎮（B拡張）               │
        ▼                                   ▼
     I4 動物ライブ統合 ──────────► I5 結果配線🎮/🖼️
                                       ▼
                              I6 パラメータ集約 ＝ MVP完成
```

- **クリティカルパス**: B → I1 → I2 → I3。A・C・D・E はこれと並行して先行完成可。
- **Claude単独完結（操作待ち不要）**: A / D / E / C生成部 / F素材 → ここを先に埋めるのが最速。
- 実行スタイル: Claudeが1本のライン内で独立ファイルをまとめ生成（共有ファイルは単一管理、uid衝突なし）。

---

## 8. フェーズ別タスク & 進捗トラッカー（都度更新）

凡例: ✅完了 / 🚧進行中 / ⬜未着手

| ID | 内容 | テスト | 状態 | メモ |
|---|---|---|---|---|
| F1 | Godot 4.7.2 導入・動作確認 | — | ✅ | OneDriveデスクトップに設置。CLI起動OK |
| F2 | project.godot/構成/.gitignore/スクショハーネス | 🖼️Claude | ✅ | デモ描画(床cube+フンsphere)確認済 |
| F3 | BalanceConfig ＋ autoload Config ＋ 契約定義 | boot | ✅ | headless boot OK。§6に契約記載 |
| A | ステージ床・壁・全体俯瞰カメラ（Stage.tscn） | 🖼️Claude | ✅ | 24×16床+4壁(wallグループ)+俯瞰カメラ。スクショ確認済 |
| B | プレイヤー移動＋フン球サイズ連動成長（Player.tscn） | 🎮ユーザー | ✅ | 操作テスト合格(2026-09-19)。矢印移動/壁停止/Space成長すべてOK |
| C | 地面ドロップフン 生成・接触回収（DungPickup.tscn） | 🖼️/🎮 | ✅ | 回収はheadless自動テストPASS(size1.0→1.5/pickup消滅)。見た目確認済。owner/despawnパラメータ配線済 |
| D | 動物3種＋ランダムスポナー（鳥/ラプトル/オランウータン投擲） | 🖼️Claude | ✅ | 基底Animal+group"dung_container"経由。**鳥=外からランダム角度(斜めあり)で横断→外へ / ラプトル=外から走り込み壁で2〜3回折り返して場内走行→外へ消滅・ドロップは1レッグ毎1〜3個で折り返し時リセット(走行全体に分散) / オランウータン=外から1個ずつランダム間隔で計2個投擲**。各headless PASS(raptor:9個/last_drop8.6s, orangutan:着弾2/間隔1.4s)。直撃効果はI3/I4で追加 |
| E | HUD＋結果画面（モックデータで先行） | 🖼️Claude | ✅ | HUD(残時間+4色サイズ)/Results(降順順位+DASH HITS#1)。見た目・ソート・表彰選定を検証。※UIはASCII(下記CJKフォント課題) |
| F | 仮SE（突進/命中/回収/スタン） | 🎮結合時 | ⬜ | 後半 |
| I1 | コアループ統合（B+A+C+タイマー+勝敗） | 🎮ユーザー | ✅ | 操作テスト合格(2026-09-19)。フン回収/成長/タイマー/結果/動物挙動すべてOK。main_scene=Game.tscn |
| I2 | 4人ローカル入力＋共有カメラ（InputMap） | 🎮ユーザー | ✅ | 実機OK(2026-09-19)。**Joy-Conは結合ペア"(L/R)"=1デバイス**。割り当ては"スティック単位"(InputRouter.sources: 1台→左右2人分)。P1=dev0左/P2=dev0右/P3=dev1左/P4=dev1右。横持ち補正: 左stick→270/右stick→90(自動)。矢印=生キー(ui_*不使用)。突進ボタン: 左=13(DpadLeft)/右=3(Y)【I3で使用】 |
| I3 | 突進システム（命中/自滅/相打ち/スタン/ドロップ） | 🎮ユーザー | ✅ | 操作テスト合格(挙動OK)。状態機械+壁自滅/相打ち/命中/分裂ドロップ/前隙白点滅/スタン灰。**突進ボタン 左=12(DpadDown)/右=3**・kbd矢印=/・WASD=Shift。headless PASS(命中/自滅/相打ち) |
| I4 | 動物ライブ統合 | 🖼️＋🎮 | ✅ | 動物3種はI1でGameに統合済。**オランウータン直撃効果=ボーナス+軽スロウ(切替 orangutan_hit_effect)** をThrownDungに実装(飛行中プレイヤー直撃→効果適用・地面フンなし)。headless PASS(size+1/slow/地面0) |
| I5 | 結果配線＋サブ表彰1つ | 🖼️/🎮 | ✅ | GameManager._end_matchで実データ(サイズ/dash_hits)→Results。dash_hitはI3で実増加。headless(game_check)で結果表示確認 |
| I6 | 全挙動を Config.balance 参照に統一 | 🖼️ | ✅ | §3の調整値(突進/ドロップ/フン/移動/動物/オランウータン/制限時間)は全てBalanceConfig参照。ステージ寸法/配置/カメラ/色は構造値としてコード固定(MVP方針) |

---

## 9. テスト分担

- **🖼️ Claude（スクショで自動確認）**: 操作を伴わない見た目・自律挙動。ステージ、動物の移動/ドロップ、UIレイアウト、フン成長の見た目 等。`tools/ScreenshotRunner` で撮影→確認。
- **🎮 ユーザー（Godotで操作テスト）**: プレイヤー操作が絡むもの。移動感、突進の命中/回避/相打ち、スタン復帰、4人同時、実機Joy-Con。→ **その都度「何をどう操作して何を確認してほしいか」をClaudeが提示する**。

---

## 10. Codex 引き継ぎメモ

1. まず本ファイルの §2 環境・§6 契約・§8 進捗表を読む。
2. `class_name` を足したら `--import` → 実行の順（§2の落とし穴）。
3. 実装は §6 の契約（シグナル/API/パラメータ名）に厳密に従う。勝手に別APIを作らない。
4. 調整値は必ず `Config.balance.*` 経由。コード直書き禁止（MVP構成 §3）。
5. 進めたら §8 の状態列と冒頭の「最終更新／現在フェーズ」を更新する。
6. 操作テストが必要な項目はユーザーに依頼（§9）。見た目確認はスクショハーネスで自走。

### 既知の課題（MVP仕上げ）
- **日本語(CJK)フォント未導入**: Godot標準フォントはCJK字形を持たないため、UI文字は暫定でASCII/ローマ字。日本語表示にはCustom Font(例: Noto Sans JP)をtheme既定フォントに設定する必要がある。
- **F 仮SE 未着手** / **サイズ連動の減速(move_speed_size_penalty)は既定オフ**。
