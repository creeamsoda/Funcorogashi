# フンコロバトル 引き継ぎ資料（Codex向け）

> 別のAIエージェント(Codex)が作業を引き継ぐための入口ドキュメント。まずこれを読み、次に
> [フンコロバトル_実装計画.md](フンコロバトル_実装計画.md)（詳細な進捗台帳・契約）、
> 仕様は [企画書](フンコロバトル_企画書.md) / [MVP構成](フンコロバトル_MVP構成.md) を参照。

最終更新: 2026-09-19

---

## 0. 30秒サマリ
- **Godot 4.7.2 / GDScript** の見下ろし3D 4人パーティーゲーム(MVP)。3Dモデルは使わず **フン=sphere / その他=cube変形**。
- **MVPのコアはほぼ完成**: コアループ / 突進の奪い合い / 動物3種 / オランウータン直撃 / 結果画面 / 4人Joy-Con入力 / パラメータ外部化 / 視認性UI。自動テスト(headless)＋実機操作テストで検証済み。
- **残りは F(仮SE) と H(一部Joy-Con振動)**（§8）。あとはプレイテストによるバランス調整。

---

## 1. 開発環境・実行方法（必読）

| 項目 | 値 |
|---|---|
| エンジン | Godot **4.7.2 stable**(標準版/GDScript。mono不使用) |
| 実行ファイル | `C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64.exe` |
| コンソール版(stdout取得) | `C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64_console.exe` |
| プロジェクト | `C:\Users\sotak\Develop\funcorogashi` |
| メインシーン | `res://scenes/Game.tscn` |

- **`godot` コマンドでは呼べない**(実行ファイル名が違うため)。上記フルパスを使う。
- **落とし穴**: `class_name` 付きスクリプトを新規追加/改名したら、**先に import を走らせる**。さもないと
  「Could not find type ... in the current scope」。
  ```powershell
  $godot = "C:\Users\sotak\OneDrive\デスクトップ\Godot_v4.7.2-stable_win64_console.exe"
  $proj  = "C:\Users\sotak\Develop\funcorogashi"
  & $godot --headless --path $proj --import          # まずこれ
  & $godot --headless --path $proj --quit-after 5    # boot確認
  ```

### テストの回し方(2系統)
- **見た目確認(操作不要)= スクショハーネス**: `tools/ScreenshotRunner.tscn` に対象シーンを渡してPNG保存。
  ```powershell
  & $godot --path $proj "res://tools/ScreenshotRunner.tscn" -- "--scene=res://scenes/Stage.tscn" "--out=C:/temp/x.png" "--frames=10"
  ```
  ※headlessだと描画されないので通常起動(ウィンドウが一瞬開く)。
- **ロジック確認= headless自動テスト**: `scenes/tests/*Check.tscn` を headless 実行(自己判定してPASS/FAILとexit code)。
  - PickupCheck / AnimalCheck / RaptorCheck / OrangutanCheck / GameCheck / InputAssignCheck /
    DashCheck / OrangutanHitCheck / DropBoundsCheck
  - 例: `& $godot --headless --path $proj "res://scenes/tests/DashCheck.tscn"`
  - ※実時間待ちのテストは `--quit-after` を付けない(フレーム高速化で早期終了するため)。パースエラーでハングした時は
    `taskkill /F /IM Godot_v4.7.2-stable_win64_console.exe`(エディタは別exeなので温存)。
- **操作テスト(コントローラ/キーが要る)= ユーザーに依頼**: `Game.tscn`(F5) の対戦感、`InputTest`/`InputCalib`(F6) の入力確認 等。

---

## 2. 現在の到達状況

凡例 ✅完了 / ⬜未

| 区分 | 状態 |
|---|---|
| ステージ(24→**36×24**に1.5倍)・全体俯瞰カメラ(半径≈30/角度62°) | ✅ |
| プレイヤー(フン転がし移動・サイズ連動成長) | ✅ |
| 地面ドロップフン(生成/回収/owner本人はスタン明けまで回収不可/size_to_scale準拠/場外に出さない/本人近くに落とさない) | ✅ |
| 動物3種(鳥=斜め横断 / ラプトル=外から走り込み折り返し走行→外へ / オランウータン=1個ずつ間隔投擲) | ✅ |
| オランウータン直撃効果(ボーナス+軽スロウ, 切替可) | ✅ |
| 突進(前隙→突進→壁自滅/相打ち/命中→スタン, 50%分裂ドロップ) | ✅ |
| 結果画面(サイズ降順順位＋サブ表彰=突進命中数1位) | ✅ |
| 4人ローカル入力(Joy-Con結合ペアをスティック単位で割当＋横持ち補正, キーボードfallback) | ✅ |
| パラメータ外部化(BalanceConfig) | ✅ |
| 視認性・予告UI(1位王冠👑→残り30秒で消去 / 突進準備は本体+フン点滅 / オランウータン着弾地点に半透明フン) | ✅ |
| **仮SE(F)** | ⬜ ← 残り |
| **一部Joy-Con振動(H：突進被弾/壁激突、相打ちは双方)** | ⬜ ← 残り |
| その他のJoy-Con振動 / BGM / 一人プレイ / 顔写真 / 被弾ズーム | ⬜(MVP対象外・拡張) |

---

## 3. アーキテクチャ地図

```
scenes/Game.tscn ── scripts/game_manager.gd (GameManager)
  ├ Stage.tscn        stage.gd  … 床/4壁(group "wall")/俯瞰カメラ/ライトをコード生成
  ├ Dung (Node3D, group "dung_container")  … 地面フンの入れ物
  ├ Player ×4         player.gd (Player)   … 移動/成長/突進状態機械/ドロップ
  ├ AnimalSpawner     animal_spawner.gd    … ランダム出現
  │   └ animals/ bird.gd raptor.gd orangutan.gd (extends Animal) / thrown_dung.gd
  └ ui/HUD.tscn hud.gd / ui/Results.tscn results.gd

autoload: Config (scripts/config.gd) … Config.balance:BalanceConfig で全調整値を参照
入力ルーティング: scripts/input_router.gd (InputRouter)
```

### 契約(信号/API) — 変更時は守る
- **Config.balance.<param>**: 全ての調整値。定義は `scripts/balance_config.gd`。
- **DungPickup**(Area3D): `size`, `owner_id`, `collectible_at`, `signal collected(size)`;
  生成は `DungPickup.create(size, owner_id)` か `GameManager.spawn_pickup(pos,size,owner_id)`。
- **Player**(CharacterBody3D): `player_id`, `size`, `facing`, `enum State{ROLLING,DASH_WINDUP,DASHING,STUNNED}`,
  `signal size_changed(id,size)`, `signal dashed(id)`, `signal dash_hit(attacker,victim)`;
  `add_size()`, `drop_and_stun(is_self_crash)`, `apply_orangutan_hit()`。group "player" に所属。
- **GameManager**: `players`, `stats[id]={dash_hits,pickups}`, `time_left`,
  `signal time_changed`, `signal match_ended(results)`; `results`要素 `{player_id,size,dash_hits}`。
- **動物**: ドロップは group "dung_container" のノードへ生成(基底 `Animal.drop_pickup`)。
- **アリーナ内寸(半分)**: 現在 **HX=18 / HZ=12**（= Stage.STAGE_SIZE 36×24 の半分）。

---

## 4. 重要な設計判断・ハマりどころ（Codex必読）

1. **Joy-Conは結合ペア"(L/R)"=1デバイス**(左stick=Lジョイコン, 右stick=Rジョイコン, ボタンも左右同居)。
   → 割当は**スティック単位**(`InputRouter.sources`): 1台につき {device,stick=0(左)} と {device,stick=1(右)} の2ソースを
   プレイヤーへ順に配る。P1=dev0左/P2=dev0右/P3=dev1左/P4=dev1右…。余りはキーボード(矢印→WASD→待機)。
2. **横持ち補正**: 左スティック→回転 **270°(-90)** / 右スティック→ **90°**(player `_joypad_vector`)。
3. **矢印キーは生キー読み**(`_arrows_vector`)。組み込み `ui_left/right/up/down` は**joypadも拾う**ため使わない
   (これを使うとキーボードPがjoypadでも動く不具合になる)。WASDも生キー。
4. **突進ボタン**: 左stick=**12(DpadDown)** / 右stick=**3(Y)**。キーボードは 矢印P=`/`, WASD P=`Shift`。
   `is_joy_known` で幻デバイスを除外、`joy_connection_changed` で起動後の増減にも再割当。
5. **アリーナ寸法は3箇所で一致必須**: `stage.gd STAGE_SIZE` / `player.gd ARENA_HX,ARENA_HZ` /
   `animals/animal.gd HX,HZ`。加えて `game_manager.gd SPAWN_SPOTS`。ステージを変える時は全部更新する
   (専用の共有定数に集約する余地あり=将来のリファクタ候補)。
6. **class_name追加後は --import**(§1の落とし穴)。
7. **テスト分担**: 見た目=スクショハーネスでClaude/Codex自走 / 操作系(移動・突進・4人・実機Joy-Con)=ユーザー依頼。
8. **カメラ調整**: `tools`ではなく `scenes/tests/CameraFrame.tscn` を `--camy/--camz` で撮り比べて `stage.gd` の
   `cam.position` を決める運用(半径固定で角度だけ変える=円周上移動)。

---

## 5. 現在のバランス調整値（ユーザーがInspectorで調整中）

`resources/balance_default.tres` の上書き(スクリプト既定からの変更):
- `size_start=10.0`, `size_gain_per_pickup=0.001`, `size_max=60.0`, `size_to_scale=0.05`,
  `drop_scatter_radius=7.0`, `raptor_speed=15.0`
- 上記以外は `scripts/balance_config.gd` の既定値。

> **重要**: `balance_default.tres` は**ユーザーが継続調整中**。Codexは勝手に上書き/revertしない。
> 数値変更の依頼が来たらこのファイルの該当行を編集(または新規上書き行を追加)する。

---

## 6. Git 状態

- ブランチ **main**。リモート未設定・未push。
- コミット履歴(新しい順):
  - `5acd1ba` ステージ1.5倍とカメラ画角の調整
  - `79db9f3` ドロップ散らばりの場外防止・自己回収防止／地面フンをsize_to_scaleに統一
  - `ffddd8d` I3/I4: 突進システムとオランウータン直撃効果
  - `95c405b` 初期コミット: フンコロバトル MVP 基盤
- **未コミット(意図的に残置)**: `resources/balance_default.tres`(ユーザー調整) / `scenes/Game.tscn`(エディタのuid保存)。
- コミット運用: プロジェクトの `.claude/skills/commit-session`(自分の変更だけ明示ステージ)/`push` を使う。
  `git add -A` は使わない。コミットメッセージ末尾に `Co-Authored-By:` 行を付ける。

---

## 7. テストシーン一覧

- **headlessロジック(自己判定)**: `scenes/tests/` の PickupCheck / AnimalCheck / RaptorCheck /
  OrangutanCheck / GameCheck / InputAssignCheck / DashCheck / OrangutanHitCheck / DropBoundsCheck / UiFeedbackCheck
- **見た目(スクショ)**: PlayerTest / PickupTest / AnimalTest / HudTest / ResultsTest / RaptorCross /
  BirdSpread / CameraFrame
- **操作(ユーザー実機)**: Game(F5) / PlayerTest / InputTest / InputCalib(F6)

---

## 8. 残タスク: F（仮SE）/ H（一部Joy-Con振動）の進め方

MVP構成 §1-8「仮のSEを付けて手触りを確認」。外部アセットは無いので**簡易生成**でよい。

- 付ける対象イベントと利用できる信号:
  - 突進開始 … `Player.dashed(id)`
  - 命中 … `Player.dash_hit(attacker,victim)`
  - 回収 … `DungPickup.collected(size)`
  - スタン/自滅 … **信号未定義**。`Player._enter_stun()` / `_self_crash()` にフック、または
    `signal stunned(id)` / `signal crashed(id)` を追加して GameManager から拾う。
- 実装案: `AudioManager`(Gameの子 or autoload) が各信号を購読し `AudioStreamPlayer` で再生。
  SE素材は (a) 手続き的に短い .wav を生成して `audio/` に置く、(b) `AudioStreamGenerator`/簡易トーン生成、のいずれか。
- H（一部Joy-Con振動）:
  - 突進被弾時に被弾プレイヤーのJoy-Conを振動。
  - 壁激突時に自滅したプレイヤーのJoy-Conを振動。
  - 相打ちは双方を被弾扱いとして双方振動。
  - 突進した側が一方的に命中したときやオランウータン直撃など、その他の振動はMVP対象外。
- BGMはMVP対象外(拡張)。

---

## 9. プレイテストで詰める論点（MVP構成 §4）

1. 回避難易度の年齢差(前隙 `dash_windup`・突進速度・ロックオン要否)
2. フンの圧迫(サイズ上限 `size_max`・伸び方 `size_growth_mode`・ステージ拡張要否)
3. 中盤の意義(大きさ連動ステータス `move_speed_size_penalty` の要否)
4. 相打ちの分かりやすさ
5. オランウータン直撃(ボーナス量/スロウ量/切替 `orangutan_hit_effect`)
6. 地面フンの自然消滅(`pickup_despawn_enabled`)の要否
7. サブ表彰の盛り上がり

---

## 10. 既知の課題
- **日本語(CJK)フォント未導入**: UI文字は暫定ASCII/ローマ字。日本語表示にはCustom Font(例 Noto Sans JP)を
  theme既定フォントに設定する必要あり。
- **アリーナ寸法の重複**(§4-5): 3ファイル+SPAWN_SPOTSに散在。共有定数への集約が望ましい。
