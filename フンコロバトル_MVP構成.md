# ワチャワチャ！フンコロバトル！ MVP構成

> ※タイトルは仮題です。実装時に参照する最小構成(Minimum Viable Product)を定義します。

---

## 0. MVPの目的とスコープ方針

- **目的**：「フンを転がして育て、突進で奪い合う」というコアの面白さが成立するかを、実プレイで検証できる最小の遊べる状態をつくる。
- **技術前提**：見下ろし視点(トップダウン)・3D。プラットフォームはUnityまたはGodot。仮プロジェクト名 `funcorogashi`(Godot / GitHub 用)。
- **含めるもの**：4人マルチプレイの対戦コアループ一式。仮のSE。順位・突進・着弾を伝える最小限の視認性UI。突進被弾/壁激突時のJoy-Con振動。
- **含めないもの**：一人プレイ(スコアアタック)、Webカメラ顔写真、被弾ズーム演出、下記以外の凝ったUI/エフェクト、ステージ拡張、全面的なJoy-Con振動対応、BGM。→ すべて拡張フェーズ/後回しへ。
- **判断基準**：見た目・演出の作り込みより、**バランスが調整可能な状態でコアループが回ること**を優先する。

---

## 1. MVPに含める機能

### 1-1. プレイ人数・時間
- 4人同時プレイ(ローカル)。
- 1プレイ3分。

### 1-2. ステージ
- 長方形・周囲壁・**固定**。1種類で可。
- ステージ拡張なし。

### 1-3. 操作(Switch Joy-Con)
- Lスティック：フンを転がして移動。
- 突進ボタン：正面方向へ突進。

### 1-4. フンの育成ループ
- 動物がステージ上にフンをドロップ。
- 転がすフンをドロップに接触させると取り込んでサイズ増加。
- サイズはスコアに直結。

### 1-5. 突進システム
- 命中：被弾側が保有フンの50%をドロップ+スタン。
- 回避成功:回避側は無傷。外した側は壁激突で自分が50%ドロップ+スタン。
- 相打ち：両者ノックバック+両者50%ドロップ。
- ドロップしたフンは、自分のスタン終了後は自分でも回収可能。
- **クールタイム・予備動作の長さはパラメータ化**(調整可能にしておく)。

### 1-6. 動物ドロップ(ランダム)
- 鳥(ゆっくり横切る)/恐竜ラプトル(素早く走り回る)/オランウータン(投げ込む)。
- 経路上・着弾点にフンをドロップ。
- 出現はランダム。
- **オランウータン直撃の効果**は暫定で1パターン実装(推奨：ボーナス+軽スロウ)、後で切替可能に。

### 1-7. 勝敗・結果表示
- メイン：一番フンが大きいプレイヤーが勝利。
- サブ表彰：最低1つ(突進命中数1位 など)は入れて結果発表を成立させる。余裕があれば追加。

### 1-8. 視認性・予告UI
- **現在1位のプレイヤー上に王冠マーク**を表示する。
  - 王冠は対戦開始から表示し、順位変動に追従する。
  - **残り30秒になった時点で非表示**にし、終盤の順位を直接知らせない。
- **突進準備中はプレイヤー本体と転がしているフンをまとめて点滅**させ、攻撃の予備動作を明確にする。
- **オランウータンが投げるフンの着弾地点に、うっすら半透明のフンを表示**し、着弾まで危険地点を予告する。

### 1-9. 音
- 突進・命中・回収・スタン等に**仮のSE**を付け、手触りを確認する。
- BGMは含めない(未定)。

### 1-10. Joy-Con振動(一部)
- プレイヤーが**突進を被弾したとき**に、そのプレイヤーのJoy-Conを振動させる。
- 自分の突進で**壁に激突したとき**に、そのプレイヤーのJoy-Conを振動させる。
- 相打ちは双方を突進被弾として扱い、双方のJoy-Conを振動させる。
- 振動時間・強さは短く控えめな仮値から調整する。
- 突進した側が一方的に命中させたときや、オランウータン直撃など、その他の振動対応はMVP外とする。

---

## 2. MVPでは実装しない(拡張フェーズ送り)

| 項目 | 送り先 |
|---|---|
| 一人プレイ(スコアアタック、NPCフンコロガシ) | 拡張 §8-1 |
| Webカメラ顔写真取り込み(選択制) | 拡張 §8-2 |
| 被弾ズーム/ふっとばし演出 | 拡張 §8-3 |
| ステージ拡張(圧迫対処の候補、MVP外) | 拡張/プレイテスト後判断 §8-4 |
| フンの供給元別の質感差(硬い/柔らかい/粘着) | 方針のみ保持 |
| 飛沫・臭気などフンらしさUIエフェクト | 方針のみ保持。§1-8の視認性・予告UIのみMVPに含む |
| 大きさ連動ステータス | 要検討、MVP後 |
| Joy-Con振動 | §1-10の突進被弾/壁激突のみMVP。その他は拡張(企画書 §9) |
| BGM | 未定(企画書 §9) |

> フンの質感・臭気UI等は「方針として保持」。MVPでは最小限の見た目に留める。

---

## 3. 調整可能にしておくべきパラメータ

プレイテストで触りたい値は、最初から外部から調整できる形にしておく。

- 突進：クールタイム、予備動作(前隙)の長さ、突進速度、突進距離、スタン時間。
- ドロップ：被弾時のドロップ割合(既定50%)、相打ち時の挙動。
- フン：サイズの伸び方(線形/対数)、サイズ上限、地面での自然消滅の有無・時間。
- 動物：出現頻度、移動速度、オランウータン直撃の効果(ボーナス量/スロウ量)。
- 全体：制限時間(既定3分)。

> 特に「回避難易度の年齢差」「大きさのリスクリターン」「フンの圧迫問題」はこれらの値で決まるため、コード直書きにせずパラメータ化しておくことが重要。

---

## 4. プレイテストで検証する主要論点

MVPを使って最優先で確認したいこと。

1. **回避難易度の年齢差**：全員が「気をつけていたら避けられるが、タイミング次第では避けられない」バランスになっているか。予備動作の長さ・ロックオン+微修正の要否を判断。
2. **フンの圧迫問題**：大きくなったフンでステージが窮屈にならないか。サイズ上限(伸びの緩やかさ)/ステージ拡張のいずれで対処するか。
3. **中盤の意義**：終盤の奪い合いが本命という方針は保ちつつ、中盤の育成が無意味になっていないか。大きさ連動ステータスの要否を判断。
4. **相打ち処理**：「両者ドロップ」で理解しやすく成立するか。
5. **オランウータン直撃**：ボーナス+軽スロウで駆け引きが成立するか。
6. **地面のフンの自然消滅**：必要か否か。
7. **サブ表彰**:結果発表が盛り上がるか、負けた人も満足できるか。

---

## 5. 実装の優先順位(目安)

1. **コアループ**：移動(フン転がし)/ドロップ発生/接触でサイズ増加/制限時間と勝敗判定。
2. **突進システム**：命中・回避(壁激突)・相打ち・スタン・50%ドロップ・回収。
3. **動物3種**のドロップ挙動(ランダム)。
4. **結果画面**：メイン順位+サブ表彰1つ。
5. **パラメータの外部調整化**(§3)。
6. **視認性・予告UI**：1位王冠、突進準備時の全体点滅、オランウータン投擲の着弾予告。
7. **仮SE＋一部Joy-Con振動**：突進被弾/壁激突の手応えを確認できる状態にする。
8. (ここまでで遊べる状態=MVP完成)
9. 以降、プレイテスト結果を反映しつつ拡張フェーズ(一人プレイ→顔写真→演出)へ。

---

## 6. 拡張フェーズの着手順(推奨)

1. **一人プレイ(スコアアタック)**：コアループを流用でき、ハイスコア掲示で学園祭と好相性。NPCロジックは将来のCPU対戦の土台にもなる。
2. **被弾ズーム/ふっとばし演出**：盛り上げ強化。酔い対策を伴う。
3. **Webカメラ顔写真(選択制)**：肖像権・データ管理の整備が前提。最も優先度低。
